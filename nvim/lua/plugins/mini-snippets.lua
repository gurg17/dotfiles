return {
    'echasnovski/mini.snippets',
    version = '*',
    config = function()
        require('mini.snippets').setup({
            snippets = {
                -- Example snippets (can be expanded later)
            },
        })
    end
}

